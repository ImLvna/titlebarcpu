{
  "name": "cpuInStatusbar",
  "version": "1.0.0",
  "main": "out/index.js",
  "scripts": {
    "build": "tsc && yarn run postbuild",
    "postbuild": "cp src/index.html out/",
    "dist": "electron-builder",
    "start": "electron out"
  },
  "build": {
    "appId": "com.luna.cpu",
    "productName": "CPU in statusbar"
  },
  "dependencies": {
    "os-utils": "^0.0.14",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "electron": "^25.3.0",
    "@types/os-utils": "^0.0.1",
    "electron-builder": "^24.4.0"
  }
}
